server {
    listen 8080;
    server_name _;

    # Habilitar compresión Gzip para mejorar el rendimiento de carga
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/javascript;
    gzip_disable "MSIE [1-6]\.";

    # Directorio raíz y archivos índice
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Redireccionar todas las peticiones a index.html para React Router (SPA)
        try_files $uri $uri/ /index.html;
    }

    # En producción (Cloud Run), el frontend NO hace proxy al backend.
    # Las llamadas a la API se hacen directamente desde el navegador del usuario
    # a la URL del backend en Cloud Run usando VITE_API_URL embebido en el build.

    # Configuración de páginas de error
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
